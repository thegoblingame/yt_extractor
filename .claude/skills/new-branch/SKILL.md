---
name: new-branch
description: Pulls the latest changes from origin to ensure main is up to date, then creates a new branch. Use when starting work on a new feature or fix.
---


Usage: Just run the new-branch script in the scripts folder. The user provides a branch name as an argument.

Example: `/new-branch "feature/add-login"`
