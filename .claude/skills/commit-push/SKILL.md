---
name: commit-push
description: Commits all changes with a provided message and pushes to remote. Use when the user wants to commit and push their changes in one step.
---


Usage: Just run the commit-push script in the scripts folder. The user provides a commit message as an argument.

Example: `/commit-push "fix login bug"`
