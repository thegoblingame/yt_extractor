Okay, so I have some corrections on the spec files describing how the scripts are going to function.

First of all, for the scripts, I actually don't want to burden the user with having to specify the input CSV files and the output folders. I would rather have every single script have some predetermined inputs and outputs which the user will modify every time they run a script. For example, for the download audio script, there will be a CSV file in the folder adjacent to the script, and the user will just modify the contents of the CSV every time they want to run the script. Similarly, the output will always go to an adjacent folder.

Also, for the combined audio script, You mentioned different possibilities for the order of combining the volume files. I was hoping that the order could just be determined by the order in which I place individual audio files to be combined into the folder with the script. is there any reason why I can't do this? 